# No provider keys are stored in env vars for the MVP.
# Users paste their API key directly into the app for the current session only.

# Optional: disable Next.js telemetry locally.
# NEXT_TELEMETRY_DISABLED=1
